apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${PIPELINE_SA}
  namespace: ${PIPELINE_NS}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ${PIPELINE_SA}-role
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log", "services", "configmaps", "secrets", "persistentvolumeclaims", "namespaces"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["tekton.dev"]
    resources: ["tasks", "taskruns", "pipelines", "pipelineruns", "pipelineresources"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ${PIPELINE_SA}-binding
subjects:
  - kind: ServiceAccount
    name: ${PIPELINE_SA}
    namespace: ${PIPELINE_NS}
roleRef:
  kind: ClusterRole
  name: ${PIPELINE_SA}-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ${PIPELINE_SA}-default-binding
subjects:
  - kind: ServiceAccount
    name: default
    namespace: ${PIPELINE_NS}
roleRef:
  kind: ClusterRole
  name: ${PIPELINE_SA}-role
  apiGroup: rbac.authorization.k8s.io
